<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishFalvorMapper">
	<insert id="addDishFlavorBatch">
    	insert into dish_flavor(dish_id,name,value) values
	    <foreach collection="flavorList" item="flavor">
	    	(#{flavor.dishId},#{flavor.name},#{flavor.value})
	    </foreach>
	</insert>
	<select id="pageQuery" resultType="com.sky.vo.DishVO">
	    select d.*,c.name as 'categoryName' from dish d left join category c on d.category_Id = c.id
	<where>
	<if test="name != null and name != ''">
	    and d.name like concat(
	    <if>
		    CONVERT(str:varchar USING ref:ref... varchar
		    Press Enter to insert, Tab to replace Next Tip
	    </if>
	    )
	</where>
	
	</select>
</mapper>
